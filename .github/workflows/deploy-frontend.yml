name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare frontend for GitHub Pages
        run: |
          # Create build directory with static subdirectory
          mkdir -p build/static

          # Copy index.html to root
          cp frontend/index.html build/

          # Copy CSS and JS to static folder
          cp frontend/style.css build/static/
          cp frontend/app.js build/static/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          publish_branch: gh-pages
